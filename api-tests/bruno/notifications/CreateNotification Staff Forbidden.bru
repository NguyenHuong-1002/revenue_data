meta {
  name: CreateNotification - Staff Forbidden (403)
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/notifications
  body: json
  headers: {
    Authorization: Bearer {{staffToken}}
    Content-Type: application/json
  }
}

body:json {
  {
    "title": "Staff test",
    "content": "Staff không được tạo thông báo",
    "type": "SYSTEM"
  }
}

tests {
  expect(res.status).to.equal(403);
}

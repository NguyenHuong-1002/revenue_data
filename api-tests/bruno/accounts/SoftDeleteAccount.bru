meta {
  name: SoftDeleteAccount
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/accounts/{{currentListUserId}}/soft
  body: none
  headers: {
    Authorization: Bearer {{adminToken}}
    Content-Type: application/json
  }
}

tests {
  expect(res.status).to.equal(200);
}
